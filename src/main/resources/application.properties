spring.application.name=demo
# Server port (optional, default 8080)
server.port=8080

# Run locally with development profile (disables auth via DevSecurityConfig)
spring.profiles.active=dev

# MySQL database connection
spring.datasource.url=jdbc:mysql://localhost:3306/mealplanerdb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
#spring.datasource.url=jdbc:mysql://localhost:3306/MealPlanerDB?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=kekokeko2005
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate / JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT Configuration
jwt.secret=mySecretKeyForJWTTokenGenerationThatIsAtLeast256BitsLongForHS256Algorithm
jwt.expiration=86400000
jwt.refresh.expiration=604800000

# Email Configuration (Gmail SMTP)
# Set email.enabled=true when you have valid Gmail App Password
email.enabled=false
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=kareemradwan2005@gmail.com
spring.mail.password=exll uosa vkug qjsv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Application URL (for email links)
app.url=http://localhost:3000

# Jackson Configuration for ISO 8601 date/time serialization
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=UTC